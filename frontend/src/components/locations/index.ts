/**
 * Barrel export for location components.
 *
 * @module components/locations
 */
export { default as LocationCard } from './LocationCard';
export { default as LocationForm } from './LocationForm';
export type { LocationData, LocationFormData } from './types';
export { emptyLocation } from './types';
