# =============================================================================
# Loyallia Backend — Python Dependencies
# Standardized: Python 3.13 + Django 5.2 LTS + Latest stable packages
# Last updated: 2026-06-02
# =============================================================================

# Core Django (LTS until April 2028)
django>=5.2,<5.3
django-ninja>=1.6,<1.7
email-validator>=2.2
django-cors-headers>=4.7

# Database — psycopg v3 (modern, faster, better async support)
psycopg[binary]>=3.2
dj-database-url>=2.3

# Cache & Queue
celery>=5.5
redis>=5.3
django-redis>=5.4
django-celery-beat>=2.7
django-celery-results>=2.6

# Async task monitoring
flower>=2.0

# File storage (MinIO / S3-compatible)
django-storages>=1.14
boto3>=1.38

# Image processing & QR
pillow>=12.0
qrcode[pil]>=8.1

# Cryptography & JWT
PyJWT>=2.10
cryptography>=44.0
argon2-cffi>=23.1
passlib>=1.7

# Apple Wallet PKPass generation
pyopenssl>=25.0

# Google OAuth / Wallet
google-auth>=2.40
httpx[http2]>=0.28

# Email
django-anymail>=13.0

# SMS (Twilio)
twilio>=9.0

# Configuration
python-decouple>=3.8

# Error tracking
sentry-sdk[django]>=2.32

# WSGI server
gunicorn>=23.0

# Utilities
python-slugify>=8.0
shortuuid>=1.0
Markdown>=3.8

# Data Processing
pandas>=2.3
openpyxl>=3.1
xlrd>=2.0

# PDF generation (invoices)
reportlab>=4.4

# Firebase Admin (for future push notifications)
firebase-admin>=6.8

# Development & Testing
django-extensions>=3.2
pytest>=8.4
pytest-cov>=6.1
pytest-django>=4.11
pytest-xdist>=3.6
